An AI-powered platform that transforms lecture videos into searchable, timestamped knowledge using speech recognition, computer vision, OCR, RAG, and agentic AI.

docker run -d --name lecture-redis -p 6380:6379 redis:7-alpine
pip install pymupdf
python -m pip install Pillow
